<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xd.app.hungroo"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="20" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.Launcher"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.GoogleLogin" />
        <activity android:name=".activity.Login" />
        <activity android:name=".activity.LocationSelector" />
        <!-- created by ADT ext plugin at 2015-11-21 06:50:56 -->
        <activity android:name=".activity.Cart" />
        <!-- created by ADT ext plugin at 2015-11-22 05:48:01 -->
        <activity android:name=".activity.CategoryListing" />
        <!-- created by ADT ext plugin at 2015-11-22 09:33:38 -->
        <activity android:name=".activity.ProductListing" />
    </application>

</manifest>